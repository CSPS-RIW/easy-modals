{
  "name": "easy-modals",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "sass-watch": "sass --watch scss/style.scss:style.css",
    "sass-build": "sass --update scss/style.scss:style.css"
  },
  "devDependencies": {
    "vite": "^2.9.9"
  },
  "dependencies": {
    "sass": "^1.52.0",
    "wicg-inert": "^3.1.1"
  }
}